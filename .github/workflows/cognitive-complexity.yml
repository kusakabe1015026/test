name: Cognitive Complexity

on:
  pull_request:
  workflow_dispatch:

jobs:
  changed-functions:
    runs-on: ubuntu-latest

    permissions:
      pull-requests: write
      contents: read

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Debug SHAs
        run: |
          echo "BASE=${{ github.event.pull_request.base.sha }}"
          echo "HEAD=${{ github.event.pull_request.head.sha }}"
          echo "GITHUB_SHA=${{ github.sha }}"

          echo
          echo "===== BASE COMMIT ====="
          git show --no-patch --stat ${{ github.event.pull_request.base.sha }}

          echo
          echo "===== HEAD COMMIT ====="
          git show --no-patch --stat ${{ github.event.pull_request.head.sha }}

          echo
          echo "===== GITHUB_SHA COMMIT ====="
          git show --no-patch --stat ${{ github.sha }}

      - name: Install clang
        run: |
          sudo apt-get update
          sudo apt-get install -y clang python3-clang

      - name: CMake configure (generate compile_commands.json)
        run: |
          cmake -S . -B build \
            -DCMAKE_EXPORT_COMPILE_COMMANDS=ON

      - name: Get changed functions
        env:
          TARGET_DIRS: "src"
        run: |
          python3 tools/get_changed_functions.py \
            "${{ github.event.pull_request.base.sha }}" \
            "${{ github.sha }}" \
            > changed_functions.json

          echo "===== changed_functions.json ====="
          ls -l changed_functions.json
          wc -c changed_functions.json
          cat changed_functions.json
          echo "=================================="

      - name: Measure complexity
        run: |
          python3 tools/measure_complexity.py changed_functions.json

      - name: Comment PR
        env:
          GH_TOKEN: ${{ github.token }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
          GITHUB_REPOSITORY: ${{ github.repository }}
          GITHUB_SHA: ${{ github.event.pull_request.head.sha }}
        run: |
          echo "===== Before comment_pr.py ====="
          pwd
          ls -la
          ls -l changed_functions.json
          wc -c changed_functions.json
          cat changed_functions.json
          cat complexity.json
          echo "================================"

          python3 tools/comment_pr.py complexity.json

